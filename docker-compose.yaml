# Local build + smoke test for the kubed/mcp-school image. `docker buildx bake`
# reads the build block below -- that is what CI runs, so the tag list and
# x-bake settings here are the release configuration, not just local convenience.
services:
  mcp-school:
    image: &image kubed/mcp-school:latest
    build:
      context: .
      target: runner
      args:
        PY_VERSION: ${PY_VERSION:-3.14}
      tags:
      - *image
      - kubed/mcp-school:${GIT_SHA:-latest}
      - kubed/mcp-school:${GIT_REF:-latest}
      - kubed/mcp-school:${GIT_TAG:-latest}
      x-bake:
        platforms:
        - linux/amd64
        - linux/arm64
        cache-to: type=gha,mode=max,scope=mcp-school
        cache-from: type=gha,scope=mcp-school
    container_name: mcp-school
    restart: no
    ports:
    - 8000:8000
    environment:
      TRANSPORT: ${TRANSPORT:-http}
      HOST: ${HOST:-0.0.0.0}
      PORT: ${PORT:-8000}
    volumes:
    - ./examples/config.yaml:/etc/mcp-school/config.yaml:ro
    - mcp-school-cache:/var/cache/mcp-school

volumes:
  mcp-school-cache:
