apiVersion: v1
kind: Service
metadata:
  name: mcp-school
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-school
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: http
